# Dependențe (NU le vrem pe GitHub)
node_modules/
mobile/node_modules/

# Fișiere de build (se generează automat)
dist/
dist-electron/
mobile/.expo/
mobile/web-build/

# Configurații locale și secrete
.env
.idea/
.vscode/
*.log

# Temporary and local test outputs
scratch/
artifacts/
release/
release-feed-test/
*.png
*.html
release.zip
*.exe
*.blockmap
latest.yml

# Database backups and exports (NEVER commit)
backups/
backups/**/*.csv
backups/**/*.sql
backups/**/*.dump
*.backup
*.sql.backup
*.csv.backup